<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:icon="@drawable/sort">

    <item android:title="SORT"
        android:icon="@drawable/sort"
        android:id="@+id/sortList"
        app:showAsAction="always"/>

    <item android:title="DELETE ALL LOCAL"
        android:id="@+id/deleteAllItemsLocally"
        app:showAsAction="never"/>

    <item android:title="DELETE ALL REMOTE"
        android:id="@+id/deleteAllItemsRemote"
        app:showAsAction="never"/>

    <item android:title="RUN SYNC"
        android:id="@+id/runSync"
        app:showAsAction="never"/>

</menu>